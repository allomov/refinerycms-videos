Refinery::Videos::Options.configure do |config|
  # Configures the maximum allowed upload size (in bytes) for an image
  # config.use_nginx_upload_module = <%= Refinery::Videos::Options::DEFAULT_USE_NGINX_UPLOAD_MODULE %>
end
